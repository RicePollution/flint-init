[service]
name = "dbus"
exec = "/usr/bin/dbus-daemon --system --nofork --nopidfile"
restart = "on-failure"

[deps]
after = ["udev"]
needs = ["udev"]

[ready]
strategy = "socket"
path = "/run/dbus/system_bus_socket"
