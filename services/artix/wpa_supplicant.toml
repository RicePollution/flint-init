[service]
name = "wpa_supplicant"
exec = "/usr/bin/wpa_supplicant -i wlan0 -c /etc/wpa_supplicant/wpa_supplicant-wlan0.conf"
restart = "never"

[deps]
after = ["udev-trigger"]

[ready]
strategy = "socket"
path = "/run/wpa_supplicant/wlan0"
