# Gentoo's net-misc/chrony installs chronyd under /usr/sbin/
[service]
name = "chronyd"
exec = "/usr/sbin/chronyd -n -d"
restart = "never"

[deps]
after = ["networkmanager"]

[ready]
strategy = "socket"
path = "/run/chrony/chronyd.sock"
