[service]
name = "bluez"
exec = "/usr/libexec/bluetooth/bluetoothd --nodetach"
restart = "never"

[deps]
needs = ["dbus", "udev-trigger"]

[ready]
strategy = "socket"
path = "/run/bluetooth/bluetooth.sock"
