[service]
name = "gamma"
exec = "/home/keegan/Projects/flint-init/services/diamond/scripts/gamma.sh"

[deps]
after = ["alpha"]
needs = ["alpha"]

[ready]
strategy = "pidfile"
path = "/tmp/flint-test/diamond/gamma.pid"
