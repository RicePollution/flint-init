[service]
name = "beta"
exec = "/home/keegan/Projects/flint-init/services/examples/scripts/beta.sh"

[deps]
after = ["alpha"]
needs = ["alpha"]

[ready]
strategy = "pidfile"
path = "/tmp/flint-test/beta.pid"
