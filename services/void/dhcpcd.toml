# Lightweight alternative to NetworkManager — common on Void minimal installs
[service]
name = "dhcpcd"
exec = "/usr/bin/dhcpcd --nobackground"
restart = "on-failure"

[deps]
needs = ["udev-trigger"]
after = ["nftables"]
