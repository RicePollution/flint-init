# System-mode PipeWire for embedded/kiosk setups; desktop installs run this as a user service
[service]
name = "pipewire"
exec = "/usr/bin/pipewire"
restart = "never"

[deps]
needs = ["dbus", "udev-trigger"]

[ready]
strategy = "socket"
path = "/run/pipewire-0"
