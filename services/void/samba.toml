# Void's samba package installs smbd under /usr/sbin/
[service]
name = "samba"
exec = "/usr/sbin/smbd --foreground --no-process-group"
restart = "on-failure"

[deps]
after = ["udev-trigger", "nftables"]

[ready]
strategy = "pidfile"
path = "/run/samba/smbd.pid"
