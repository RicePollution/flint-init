[service]
name = "docker"
exec = "/usr/bin/dockerd"
restart = "on-failure"

[deps]
after = ["containerd", "nftables"]

[ready]
strategy = "socket"
path = "/run/docker.sock"

[resources]
oom_score_adj = -500
