[service]
name = "hyprland"
exec = "env WLR_RENDERER=pixman WLR_NO_HARDWARE_CURSORS=1 /usr/bin/hyprland"
restart = "never"

[deps]
needs = ["dbus", "polkit"]
after = ["udev-trigger", "pipewire"]

[ready]
strategy = "socket"
path = "/run/user/1000/hypr"
