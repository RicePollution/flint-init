[service]
name = "dunst"
exec = "/usr/bin/dunst"
# user = "your-username"  # desktop service: set to your login user
restart = "on-failure"

[deps]
needs = ["dbus"]
after = ["hyprland"]

[ready]
strategy = "socket"
path = "/run/user/1000/dunst/dunst.sock"
